---
import '../styles/global.css';
import { SITE_DESCRIPTION } from '@/consts';
import BaseHead from '@/layouts/_BaseHead.astro';

const dateModified = '2025-09-06T12:00:00+08:00';
---

<script is:inline>
	const getThemePreference = () => {
		if (typeof localStorage !== 'undefined' && localStorage.getItem('theme')) {
			return localStorage.getItem('theme');
		}
		return window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
	};
	const isDark = getThemePreference() === 'dark';
	document.documentElement.classList[isDark ? 'add' : 'remove']('dark');

	if (typeof localStorage !== 'undefined') {
		const observer = new MutationObserver(() => {
			const isDark = document.documentElement.classList.contains('dark');
			localStorage.setItem('theme', isDark ? 'dark' : 'light');
		});
		observer.observe(document.documentElement, { attributes: true, attributeFilter: ['class'] });
	}
</script>

<html lang="en">

  <BaseHead description={SITE_DESCRIPTION} dateModified={dateModified} />
	<body>
	  <main class="md:max-w-3xl md:mx-auto px-4 md:px-0 pt-6"><slot /></main>
	</body>
</html>
